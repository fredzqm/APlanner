@model IEnumerable<APlanner.Database.Professor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Department.DepartNAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DepartNAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PUserID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PUserID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PUserID })
        </td>
    </tr>
}

</table>
